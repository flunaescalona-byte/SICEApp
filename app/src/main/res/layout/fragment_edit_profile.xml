<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary">

    <!-- HEADER -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="12dp"
        android:paddingTop="40dp"
        android:background="@color/bg_secondary"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@android:drawable/ic_media_previous"
            android:background="@drawable/btn_outline"
            android:tint="#64748b"
            android:layout_marginEnd="12dp"/>

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="✏️ Editar Perfil"
            android:textColor="@color/text_primary"
            android:textSize="17sp"
            android:textStyle="bold"/>

        <Button
            android:id="@+id/btnSave"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="Guardar"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            android:background="@drawable/btn_primary"
            android:paddingHorizontal="16dp"/>
    </LinearLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- FOTO -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/card_bg"
                android:padding="20dp"
                android:layout_marginBottom="16dp">

                <ImageView
                    android:id="@+id/imgPhoto"
                    android:layout_width="100dp"
                    android:layout_height="100dp"
                    android:scaleType="centerCrop"
                    android:background="@drawable/circle_bg"
                    android:clipToOutline="true"
                    android:src="@mipmap/ic_launcher"
                    android:layout_marginBottom="12dp"
                    android:clickable="true"
                    android:focusable="true"/>

                <Button
                    android:id="@+id/btnChangePhoto"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:text="📷 Cambiar foto"
                    android:textColor="@color/accent_blue"
                    android:textSize="12sp"
                    android:background="@drawable/btn_outline"
                    android:paddingHorizontal="16dp"/>
            </LinearLayout>

            <!-- NOMBRE -->
            <TextView style="@style/FormLabel" android:text="NOMBRE COMPLETO *"/>
            <EditText
                android:id="@+id/etName"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:textColor="@color/text_primary"
                android:hint="Ej: CB1. JUAN PÉREZ SOTO"
                android:textColorHint="#475569"
                android:textSize="14sp"
                android:padding="12dp"
                android:background="@drawable/input_form_bg"
                android:inputType="textCapWords"
                android:layout_marginBottom="4dp"/>

            <!-- CARGO -->
            <TextView style="@style/FormLabel" android:text="GRADO Y CARGO"/>
            <EditText
                android:id="@+id/etPosition"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:textColor="@color/text_primary"
                android:hint="Ej: Cabo Primero - Técnico en Informática"
                android:textColorHint="#475569"
                android:textSize="14sp"
                android:padding="12dp"
                android:background="@drawable/input_form_bg"
                android:inputType="textCapSentences"
                android:layout_marginBottom="4dp"/>

            <!-- BIO -->
            <TextView style="@style/FormLabel" android:text="DESCRIPCIÓN / BIO"/>
            <EditText
                android:id="@+id/etBio"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:hint="Descripción o información adicional..."
                android:textColorHint="#475569"
                android:textSize="14sp"
                android:padding="12dp"
                android:background="@drawable/input_form_bg"
                android:inputType="textMultiLine"
                android:minLines="3"
                android:gravity="top"
                android:layout_marginBottom="4dp"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
